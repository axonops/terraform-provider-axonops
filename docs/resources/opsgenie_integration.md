---
page_title: "axonops_opsgenie_integration Resource - axonops"
subcategory: "Integrations"
description: |-
  Manages an OpsGenie integration for AxonOps alerting.
---

# axonops_opsgenie_integration (Resource)

Manages an OpsGenie integration for AxonOps alerting.

Use this resource to configure an OpsGenie API key that AxonOps uses to create alerts when conditions are triggered. Once configured, pair the integration with an `axonops_alert_route` resource to control which alert categories and severities are forwarded to OpsGenie.

## Example Usage

```terraform
resource "axonops_opsgenie_integration" "oncall" {
  cluster_name = "production-cassandra"
  cluster_type = "cassandra"
  name         = "opsgenie-oncall"
  opsgenie_key = var.opsgenie_api_key
}

# Route node-level alerts to OpsGenie
resource "axonops_alert_route" "opsgenie_nodes" {
  cluster_name     = axonops_opsgenie_integration.oncall.cluster_name
  cluster_type     = axonops_opsgenie_integration.oncall.cluster_type
  integration_name = axonops_opsgenie_integration.oncall.name
  integration_type = "opsgenie"
  type             = "nodes"
  severity         = "error"
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `cluster_name` (String) The name of the cluster.
- `cluster_type` (String) The cluster type (cassandra, kafka, or dse).
- `name` (String) The name of the integration.
- `opsgenie_key` (String, Sensitive) The OpsGenie API key.

### Read-Only

- `id` (String) The integration ID.

## Import

OpsGenie integrations can be imported using the format `cluster_type/cluster_name/name`:

```shell
terraform import axonops_opsgenie_integration.example cassandra/production-cassandra/opsgenie-oncall
```

Where:
- `cluster_type` - The type of cluster (`cassandra`, `kafka`, or `dse`)
- `cluster_name` - The name of the cluster
- `name` - The name of the integration
